Привет)
Вчера засиделся за обработкой ошибок и случайно залил не тот код в first_view.py
Сейчас там нужная версия создания первой витрины, посмотри, пожалуйста

Что касается второй вьюхи, ты прав в коде

def month_data(data, old_registration):
    messages_month_registration = data \
        .where("event_type = 'message'") \
        .select('user_id', 'zone_id', 'date') \
        .join(old_registration, 'user_id', 'leftanti')

leftanti нужно было заменить на inner, это моя невнимательность
Первоначально задумывалося план: залить новых пользователей, через leftanti отфильтровать тех, кто уже есть в стороннем представлении
Потом возникла идея, выделить этот процесс в отдельную стадию
Все поправил, а вот способ соединения забыл, сорри

Замечания реализованы в коде. Жду проверки, особенно первой витрины!